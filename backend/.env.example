PORT=8888
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=password
DB_NAME=gudang
JWT_SECRET=your-super-secret-jwt-key

# Email Configuration
# Leave ALL fields empty for development mode (emails will be logged to console only)

# === For Mailtrap (Email Testing - Recommended for Development) ===
# Get your credentials from https://mailtrap.io/inboxes
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USERNAME=6c01b71b678c1a
SMTP_PASSWORD=d89a78b013b9ab
FROM_EMAIL=noreply@inventory.com
FROM_NAME=Inventory System

# === For Gmail (Production) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-gmail-app-password
# FROM_EMAIL=noreply@inventory.com
# FROM_NAME=Inventory System
